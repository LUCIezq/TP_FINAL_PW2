<div class="container text-center py-5">
    <h2 class="mb-4">🎡 Gira la ruleta para descubrir el genero de tu preguta</h2>

    <button id="btnGirar" class="btn btn-lg btn-primary px-5 py-3">
        Girar ruleta
    </button>
    <div id="resultado" class="mt-5 h4"></div>
</div>

<script>
    const generos = ["Historia", "Ciencia", "Geografia", "Deportes", "Arte", "Entretenimiento"];
    document.getElementById("btnGirar").addEventListener("click", () => {
        const genero = generos[Math.floor(Math.random() * generos.length)];
        document.getElementById("resultado").innerHTML =
                "🎯 Te toco: <strong>" + genero + "</strong><br><br>" +
                "<form method='POST' action='/game/start'>" +
                "<input type='hidden' name='genero' value='" + genero + "'>" +
                "<button type='submit' class='btn btn-success mt-3'>Comenzar partida</button>" +
                "</form>";
    });
</script>