<!DOCTYPE html>
<html lang="es">

<head>
  <meta charset="UTF-8">
  <title>Questify</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">

  <!-- Bootstrap -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">

  <!-- CSS global del sitio -->
  <link href="/public/assets/header.css" rel="stylesheet">
  <link href="/public/assets/footer.css" rel="stylesheet">

  <!-- CSS opcional de cada vista (si corresponde) -->
  {{{cssExtra}}}
</head>

<body class="layout">

<header class="site-header" role="banner">
  <div class="site-header__bar">
    <div class="container py-3 d-flex align-items-center justify-content-between">

      <a href="/home/index" class="site-title m-0">Questify</a>

      {{#isLogged}}
      <div class="d-flex align-items-center gap-3">

        {{#isPlayer}}
        <img src="{{usuario.foto_perfil}}" width="40" height="40" class="rounded-circle object-fit-cover">
        <span class="fw-semibold">
          Bienvenido,
          <a href="/usuario/perfil/{{usuario.id}}" class="text-decoration-none">{{usuario.nombre_usuario}}</a>
        </span>
        {{/isPlayer}}

        {{^isPlayer}}
        <a class="btn btn-sm btn-primary" href="/login/logout">Cerrar sesión</a>
        {{/isPlayer}}

      </div>
      {{/isLogged}}

    </div>
  </div>
</header>
