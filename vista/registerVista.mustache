<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Registro</title>

  <!-- Bootstrap 5 -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <!-- CSS externo -->
  <link href="/public/assets/register.css" rel="stylesheet">
  

</head>
<body>

  <!-- MAIN -->
  <main class="container my-4">
    <h2 class="page-title text-center mb-4">Registro de usuario</h2>

    <div class="row justify-content-center">
      <div class="col-12 col-lg-8">
        <div class="card card-peach shadow-lg">
          <div class="card-body p-4 p-lg-5">


              {{#errors}}
              <div class="alert alert-danger" role="alert">
                <ul class="mb-0" style="list-style:none;">
                    <li>{{.}}</li>
                </ul>
              </div>
              {{/errors}}
              


            <form action="/register/userRegister" method="POST" enctype="multipart/form-data" class="row g-3">

              {{#data.campos}}
              <div class="col-6 md-6">
                <label for="{{id}}" class="form-label">{{name}}</label>
                <input
                  placeholder="{{placeholder}}"
                  type="{{type}}"
                  id="{{id}}"
                  name="{{name}}"
                  class="form-control"
                  {{#required}}required{{/required}}>
              </div>
              {{/data.campos}}

              <div class="col-md-6">
                <label for="gender" class="form-label">Género</label>
                <select id="gender" name="gender" required class="form-select">
                  {{#genders}}
                    <option value="{{id}}">{{nombre}}</option>
                  {{/genders}}
                </select>
              </div>

              <div class="col-100 mt-2">
                <h6 class="section-title">Seleccione su ubicación en el mapa</h6>
                <div id="map" class="map-box"></div>
              </div>

              <div class="col-md-6">
                <label for="pais" class="form-label">País</label>
                <input readonly value="Argentina" type="text" id="pais" name="pais" class="form-control">
              </div>

              <div class="col-md-6">
                <label for="ciudad" class="form-label">Ciudad</label>
                <input readonly value="Buenos Aires" type="text" id="ciudad" name="ciudad" class="form-control">
              </div>

              <div class="col-12 d-grid mt-2">
                <button type="submit" class="btn btn-palette btn-lg">Registrarse</button>
              </div>

            </form>

          </div>
        </div>
      </div>
    </div>
  </main>

</body>
</html>